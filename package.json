{
  "name": "product-backlog-backend",
  "version": "1.0.0",
  "description": "Backend server for Product Backlog project",
  "main": "Backend/server.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon Backend/server.js",
    "start": "cross-env NODE_ENV=production node Backend/server.js",
    "build": "npm install && npm install --prefix Frontend --include=dev && npm run build --prefix Frontend"
  },
  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.9.4"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^3.1.14"
  }
}
